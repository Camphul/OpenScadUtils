module lib_help() {
    info("LIBRARY HELP:");
    info("honeycombs(d, dimensions, wall, inverse): create honeycomb pattern");
    info("hexagon(d): create 2d hexagon with diameter d");
    info("lib_round(value, method): round number with either FLOOR or CEILING");
    info("spray_pattern(size, move_patterns) spray a patterns");
    info("rounded_square(size, r, corners) spray a patterns");
    info("rounded_cube(size, r, corners) spray a patterns");
}
